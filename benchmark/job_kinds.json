{
  "mmlupro": {
    "description": "MMLU-Pro on one 100-question subset, one model.",
    "params": {
      "model":    {"enum": ["e2b", "e4b"]},
      "subset":   {"enum": ["s1", "s2", "s3"]},
      "thinking": {"enum": ["off", "on"], "default": "off"}
    },
    "label": "mmlupro-{model}-{subset}",
    "output_dir": "mmlupro100-{model}-{subset}",
    "suffix_when": {"thinking": {"on": "-think"}},
    "baseline": {"thinking": {"off": "mmlupro-baseline", "on": "mmlupro-thinking-on"}},
    "memory_mb": {"e2b": 3900, "e4b": 5400},
    "boards": {
      "pi": {
        "command": "env SUBSET={subset} THINKING={thinking} ./std_mmlupro.sh {model}",
        "env": {}
      },
      "jetson": {
        "command": "env SUBSET={subset} THINKING={thinking} ./std_mmlupro_jetson.sh {model}",
        "env": {"SRVLOG": "{stdbench}/{output_dir}/server.log", "SUBSET": "{subset}"},
        "memory_mb": {"e4b": 5200},
        "_memory_mb": "E4B on the Jetson peaks at +5,008 MB (worst of 4 runs, baseline and thinking). 5,400 left ~400 MB spare and refused real idle boards at 5,257, costing 10.7 h on 2026-09-24; thinking runs started at 5,238-5,239 have bottomed at 354-441 MB free. 5,200 keeps ~190 MB over the worst peak; raise it again if a run ever comes closer."
      }
    }
  },

  "mmlupro-lg": {
    "description": "S3: MMLU-Pro through little-gemma (CUDA int8), one 100-question subset, one model. Jetson only: little-gemma's fast path is CUDA.",
    "params": {
      "model":    {"enum": ["e2b", "e4b"]},
      "subset":   {"enum": ["s1", "s2", "s3"]},
      "thinking": {"enum": ["off", "on"], "default": "off"}
    },
    "label": "mmlupro-lg-{model}-{subset}",
    "output_dir": "mmlupro100-lg-{model}-{subset}",
    "suffix_when": {"thinking": {"on": "-think"}},
    "baseline": {"thinking": {"off": "lg-baseline", "on": "lg-thinking-on"}},
    "memory_mb": {"e2b": 2600, "e4b": 4400},
    "_memory_mb": "Pilots 2026-09-25 (14 questions each): E2B peaked at +2,340 MB over idle, E4B thinking-on at +3,990 MB (5,898 MB used of 7,485). The engine does not mmap and refuses to load when the tensor data exceeds available memory, so E4B needs ~4.2 GB free regardless. Tighten after the first full runs.",
    "boards": {
      "jetson": {
        "command": "env SUBSET={subset} THINKING={thinking} ./std_mmlupro_lg_jetson.sh {model}",
        "env": {"SRVLOG": "{stdbench}/{output_dir}/server.log", "SUBSET": "{subset}"}
      }
    }
  },

  "raw": {
    "description": "Any command, wrapped in run_measured.sh. The escape hatch.",
    "params": {
      "label":   {"pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$"},
      "command": {"free_text": true}
    },
    "label": "{label}",
    "output_dir": "{label}",
    "baseline": null,
    "boards": {
      "pi":     {"command": "{command}", "env": {}},
      "jetson": {"command": "{command}", "env": {}}
    }
  }
}
