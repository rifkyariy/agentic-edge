{
  "device": "raspi5",
  "engine": "proposed",
  "proposed_url": "http://192.168.1.233:8090",
  "model": { "name": "gemma4-e4b", "quant": "Q4_K_XL" },
  "cuda": false,
  "mtp": false,
  "thinking": false,
  "repeat": 3,
  "timeout_s": 180,
  "_note": "mtp/thinking are toggled LIVE here via POST /option before the run starts (see configure_proposed() in adapters.py) — the only engine that can do this without touching the device by hand. Requires the full voice-agent stack already deployed and running at proposed_url."
}
